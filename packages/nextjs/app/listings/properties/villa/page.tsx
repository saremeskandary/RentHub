import { Metadata } from "next";

export const metadata: Metadata = {
    title: `villa`,
};

export default function VillaPage() {
    
    return (
        <>
            <main>
                <h1>This is villa page</h1>
            </main>
        </>
    );
}