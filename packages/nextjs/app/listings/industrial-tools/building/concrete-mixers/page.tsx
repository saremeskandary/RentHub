import { Metadata } from "next";

export const metadata: Metadata = {
    title: `concrete-mixers`,
};

export default function ConcreteMixersPage() {
    
    return (
        <>
            <main>
                <h1>This is concrete-mixers page</h1>
            </main>
        </>
    );
}