import { Metadata } from "next";

export const metadata: Metadata = {
    title: `boat`,
};

export default function BoatPage() {
    
    return (
        <>
            <main>
                <h1>This is boat page</h1>
            </main>
        </>
    );
}