import { Metadata } from "next";

export const metadata: Metadata = {
    title: `chat-settings`,
};

export default function ChatSettingsPage() {
    
    return (
        <>
            <main>
                <h1>This is chat-settings page</h1>
            </main>
        </>
    );
}