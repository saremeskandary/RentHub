import { Metadata } from "next";

export const metadata: Metadata = {
    title: `my-city`,
};

export default function MyCityPage() {
    
    return (
        <>
            <main>
                <h1>This is myCity page</h1>
            </main>
        </>
    );
}