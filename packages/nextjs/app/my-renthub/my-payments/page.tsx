import { Metadata } from "next";

export const metadata: Metadata = {
    title: `my-payments`,
};

export default function MyPaymentsPage() {
    
    return (
        <>
            <main>
                <h1>This is my-payments page</h1>
            </main>
        </>
    );
}